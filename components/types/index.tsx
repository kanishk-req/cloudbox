export type datatype = {
  id: number;
  name: string;
  url: string;
};

export type themeType = {
  primary: string;
  secondary: string;
  accent: string;
  text: string;
  secondaryText?: string;
};
